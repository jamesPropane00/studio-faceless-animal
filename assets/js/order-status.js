import{ supabase }from"./supabase-client.js";const p=new URLSearchParams(location.search),order=p.get("order"),token=p.get("token"),money=c=>`$${(c/100).toFixed(2)}`,safe=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));let tries=0;
async function check(){const{data,error}=await supabase.rpc("get_order_status",{p_order:order,p_token:token});const o=data?.[0];if(error||!o){document.querySelector("#result-title").textContent="We couldn’t verify this order";document.querySelector("#order-result").textContent="Use the exact link from checkout or contact the studio.";return}const paid=["paid","shipped","completed","refunded"].includes(o.status);document.querySelector("#result-title").textContent=paid?`Payment verified · ${o.order_number}`:`Payment status: ${o.status}`;document.querySelector("#order-result").innerHTML=`<p>${o.items.map(i=>`${i.quantity}× ${safe(i.title)} — ${money(i.unit_price_cents*i.quantity)}`).join("<br>")}</p><p><strong>${money(o.total_cents)}</strong> · ${o.fulfillment_method==="pickup"?"Local pickup":"Shipping"}</p><p>Status: <strong>${o.status}</strong></p>`;if(!paid&&tries++<8)setTimeout(check,2500);if(paid)localStorage.removeItem("fas_shop_cart")}check();
